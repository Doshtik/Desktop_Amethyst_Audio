<Page x:Class="Desktop_Amethyst_Audio.Views.Pages.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Desktop_Amethyst_Audio.Views.Pages"
      mc:Ignorable="d"
      Title="SearchPage" d:DesignHeight="600" d:DesignWidth="800">
    <StackPanel Orientation="Vertical"> 
        <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0"  FontSize="16"/>
            <Button Grid.Column="1" Content="Поиск" Padding="5" FontSize="16"/>
        </Grid>
        <Grid Margin="10 0 10 10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="Новые альбомы:" FontSize="16" />
            <ItemsControl Grid.Row="1" ItemsSource="{Binding NewAlbums}">
                <!-- 1. Делаем список горизонтальным -->
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <!-- 2. Добавляем прокрутку -->
                <ItemsControl.Template>
                    <ControlTemplate TargetType="ItemsControl">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                                      VerticalScrollBarVisibility="Disabled">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </ItemsControl.Template>

                <!-- Используем наш UserControl -->
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!--<views:AlbumCard />-->
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        
        <Grid Margin="10 0 10 10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Ваши любимые жанры" FontSize="16"/>
            <ItemsControl Grid.Row="1">
                
            </ItemsControl>
        </Grid>
    </StackPanel>
</Page>
